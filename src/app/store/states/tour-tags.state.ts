// import { TourTagModel } from '../../models/tag-model';
// import { createEntityAdapter, EntityAdapter, EntityState } from '@ngrx/entity';

// export interface TourTagsState extends EntityState<TourTagModel> {
//   selectedTagId: string | null;
//   isSuccess: boolean;
// }

// export const adapter: EntityAdapter<TourTagModel> = createEntityAdapter<TourTagModel>({
//   selectId: (tag: TourTagModel) => tag.id
// });

// export const initialState: TourTagsState = adapter.getInitialState({
//   selectedTagId: null,
//   isSuccess: false
// });
